import type { PropsWithChildren } from "react";
type FormRowProps = {
    hasError?: boolean;
};
export default function FormRow({
    children,
    hasError,
}: PropsWithChildren<FormRowProps>): import("react/jsx-runtime").JSX.Element;
export {};
